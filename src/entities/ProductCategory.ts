// Pure ProductCategory entity (application data structure)
export type ProductCategory = {
  id: number;
  name: string;
  shippingCost: number;
  createdAt: string;
  updatedAt: string;
  publishedAt: string;
};